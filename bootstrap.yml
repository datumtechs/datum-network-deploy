--- 

# 检查各个节点信息
- name: check system
  hosts: all
  any_errors_fatal: true
  roles:
    - check_system_static

# # 安装 python 
# - name: install python
#   hosts: all
#   tasks:
#    - shell: sudo apt-get -y install python
